# Production build env (Vite)
#
# IMPORTANT: These values are baked into the JS bundle at build time.
#
# ✅ This must point to the PUBLIC API base URL (Fastify server), NOT the MySQL port (3306).
# Example (recommended):
#   VITE_API_BASE_URL=https://api.yourdomain.com
#
# Set this to your PUBLIC API base URL.
# Recommended (store-ready):
#   VITE_API_BASE_URL=https://api.yourdomain.com
# Temporary testing-only (not store-ready):
#   VITE_API_BASE_URL=http://88.247.35.107:8787
VITE_API_BASE_URL=https://api.nelit.com.tr
